#include <bits/stdc++.h>

typedef long long ll;

using namespace std;

int main(void) {
	// For fast IO
	cin.tie(NULL);
	ios_base::sync_with_stdio(false);

	int tc;

	cin >> tc;

	while(tc--) {
		int sze;
		cin >> sze;

		int arr[sze];
		for(int i = 0; i < sze; i++) {
			cin >> arr[i];
		}

		ll p = 0;
		int psze = 0;
		bool checkplus = true;

		for(int i = 0; i < sze; i++) {
		}
	}

	return 0;
}